import FeedbackList from '@/components/tables/FeedbackList';

export default function GuestsPage() {
    return (
        <div className="fade-in">
            <FeedbackList />
        </div>
    );
}